[project]
name = "wildedge-cli-demo"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "wildedge-sdk",
    "torch",
    "timm",
]

[tool.uv.sources]
wildedge-sdk = { path = "../..", editable = true }
